package com.keller23.android.aorb.lib;

public class Analyzers {
/*
    public static enum Trackers {
        AppTracker, // Just for this app
        AndroidTracker, // All Android apps
        EverythingTracker, // For everything
    }

    HashMap<Trackers, Tracker> mTrackers = new HashMap<Trackers, Tracker>();

    synchronized Tracker getTracker(Trackers trackerId) {
        if (!mTrackers.containsKey(trackerId)) {

            GoogleAnalytics analytics = GoogleAnalytics.getInstance(this);
            Tracker t = (trackerId == Trackers.AppTracker) ? analytics.newTracker(PROPERTY_ID)
                    : (trackerId == Trackers.AndroidTracker) ? analytics.newTracker(R.xml.androidTracker)
                    : analytics.newTracker(R.xml.everythingTracker);
            mTrackers.put(trackerId, t);

        }
        return mTrackers.get(trackerId);
    }*/
}
